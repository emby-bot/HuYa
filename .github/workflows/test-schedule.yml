name: 测试定时触发

"on":
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 打印时间
        run: |
          echo "event=${{ github.event_name }}"
          date
          echo "schedule test ok"
